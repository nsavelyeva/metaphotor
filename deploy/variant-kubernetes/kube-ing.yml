apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: kube-ingress
  annotations:
spec:
  rules:
  - host: metaphotor.info
    http:
      paths:
      - path: /
        backend:
          serviceName: nginx
          servicePort: 80
      - path: /
        backend:
          serviceName: flask
          servicePort: 5000